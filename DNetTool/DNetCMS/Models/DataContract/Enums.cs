namespace DNetCMS.Models.DataContract
{
    public class Enums
    {
        public enum ObjectiveType { Search = 1, Sort, Calculate, Generate }     

        public enum FileType { Picture = 0, Document, ToStore }
    }
}